/*
vim: syntax=groovy
-*- mode: groovy;-*-
 * -------------------------------------------------
 *  Nextflow base config file
 * -------------------------------------------------
 */

params {
  max_memory = 50.GB
  max_cpus = 10
  max_time = 240.h
}

executor{
   jobName = { "$task.tag" }
}
